# Staging environment overrides for Kamal
# Inherits from config/deploy.yml

servers:
  web:
    - 5.161.78.78

# Enable SSL for Cloudflare Strict mode
proxy:
  ssl: true
  host: staging.picoletter.com

env:
  clear:
    RAILS_ENV: production
    SOLID_QUEUE_IN_PUMA: true
    FORCE_SSL: true
    PICO_HOST: https://staging.picoletter.com
