<div class="max-w-md py-20 mx-auto">
  <h1 class="text-3xl text-stone-900">Welcome back 👋</h1>
  <p class="mt-2 text-xl text-stone-700">Sign in with your email address and password to continue or
    <%= link_to "create a new account.",
    "/signup",
    class: "text-zinc-500 hover:underline" %></p>
  <div
    class="
      p-6 pt-5 mt-6 bg-white border shadow-sm rounded-xl pb-7 border-neutral-200
    "
  >
    <%= form_with url: auth_login_path, class: "flex flex-col gap-4" do |form| %>
      <label class="space-y-1">
        <%= form.label :email, class: "ml-px text-stone-500 font-sans" %>
        <%= form.email_field :email,
                         required: true,
                         class: "input w-full",
                         autofocus: true,
                         autocomplete: "email",
                         placeholder: "you@example.com",
                         value: params[:email] %>
      </label>

      <label class="space-y-1">
        <%= form.label :password, class: "ml-px text-stone-500 font-sans" %>
        <%= form.password_field :password,
                            required: true,
                            class: "input w-full",
                            autocomplete: "current-password",
                            placeholder: "··············" %>
      </label>

      <div class="mt-1">
        <%= form.submit "Log in", class: "btn btn-primary w-full" %>
      </div>
    <% end %>

    <%= render partial: "shared/social_buttons", locals: { label: "Or sign in with" } %>
  </div>
  <div class="mt-3 space-y-2">
    <p><%= link_to "Forgot your password?",
      "/passwords/new",
      class: "text-zinc-500 hover:underline" %></p>
  </div>
</div>
