<div
  class="
    border border-stone-200 bg-stone-100 rounded-2xl p-5 -mx-5 font-sans flex gap-5
  "
>
  <div class="flex-grow">
    <h3 class="text-lg font-semibold text-stone-800 -mt-0.5">Delete this post</h3>
    <p class="text-stone-500">This is an unpublished post, deleting this means you will lose this
      forever.</p>
  </div>
  <div class="flex-shrink-0">
    <%= link_to "Delete forever",
    post_path(slug: post.newsletter.slug, id: post.id),
    class: "btn btn-danger",
    data: {
      turbo_method: :delete,
      turbo_confirm: "Are you sure you want to delete this post?",
    } %>
  </div>
</div>
