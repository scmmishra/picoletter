<% if !Current.user.subscribed? && @newsletter.can_write?(:billing) %>
  <%= render "newsletters/partials/sidebar_callout",
  title: "Subscribe to start sending emails.",
  description: [
    "Plan starts at $5, you pay only for the emails you send.",
    "Cancel any time."
  ],
  button_text: "Open billing",
  button_path: settings_billing_path(slug: @newsletter.slug),
  button_style: "btn-primary" %>
<% end %>
