<!DOCTYPE html>

<html lang="en">
  <head>
    <% font_family = @newsletter&.font_preference.presence || "sans-serif" %>
    <% accent_color = "#111111" %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <%= yield :head %>
    <style>
      body {
        font-family: <%= font_family %>;
        font-size: 14px;
        line-height: 1.4;
        margin: 0;
        -webkit-font-smoothing: antialiased;
        -ms-text-size-adjust: 100%;
        -webkit-text-size-adjust: 100%;
      }
      .container {
        max-width: 600px;
        display: block;
      }
      p, div {
        font-family: <%= font_family %>;
        font-size: 14px;
        margin-bottom: 14px;
      }
      .content {
        --highlight-1: rgb(136, 118, 38);
        --highlight-2: rgb(185, 94, 6);
        --highlight-3: rgb(207, 0, 0);
        --highlight-4: rgb(216, 28, 170);
        --highlight-5: rgb(144, 19, 254);
        --highlight-6: rgb(5, 98, 185);
        --highlight-7: rgb(17, 138, 15);
        --highlight-8: rgb(148, 82, 22);
        --highlight-9: rgb(102, 102, 102);
        --highlight-bg-1: rgba(229, 223, 6, 0.3);
        --highlight-bg-2: rgba(255, 185, 87, 0.3);
        --highlight-bg-3: rgba(255, 118, 118, 0.3);
        --highlight-bg-4: rgba(248, 137, 216, 0.3);
        --highlight-bg-5: rgba(190, 165, 255, 0.3);
        --highlight-bg-6: rgba(124, 192, 252, 0.3);
        --highlight-bg-7: rgba(140, 255, 129, 0.3);
        --highlight-bg-8: rgba(221, 170, 123, 0.3);
        --highlight-bg-9: rgba(200, 200, 200, 0.3);
        font-family: <%= font_family %>;
      }
      .content * {
        font-family: <%= font_family %> !important;
      }
      .content code,
      .content pre,
      .content kbd,
      .content samp {
        font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, Liberation Mono, Courier New, monospace !important;
      }
      /* Keep content links on the chosen accent instead of newsletter theme colors. */
      .content a {
        color: <%= accent_color %> !important;
        text-decoration: underline;
      }
      /* Keep list spacing predictable in email clients and preserve nesting indentation. */
      .content ol,
      .content ul {
        margin: 0 0 14px 1.5em;
        padding: 0;
      }
      /* Lexxy uses marker-less nested wrapper list items; avoid duplicate bullets/numbers. */
      .content li.lexxy-nested-listitem {
        list-style-type: none;
      }
      .content li.lexxy-nested-listitem > ol,
      .content li.lexxy-nested-listitem > ul {
        margin-bottom: 0;
      }
      /* Recreate the expected quote appearance with a leading border. */
      .content blockquote {
        border-left: 4px solid #d6d3d1;
        margin: 14px 0;
        padding: 8px 12px;
        font-style: italic;
      }
      /* Remove paragraph spacing noise inside quotes and table cells. */
      .content blockquote p,
      .content td p,
      .content th p {
        margin-top: 0;
        margin-bottom: 0;
      }
      .content blockquote p:last-child {
        margin-bottom: 0;
      }
      /* Add explicit table styles because email rendering does not load Lexxy stylesheet. */
      .content figure.lexxy-content__table-wrapper {
        margin: 14px 0;
        overflow-x: auto;
      }
      .content table {
        border-collapse: collapse;
        border-spacing: 0;
        width: 100%;
        margin: 0;
      }
      .content th,
      .content td {
        border: 1px solid #e7e5e4;
        padding: 8px;
        text-align: left;
        vertical-align: top;
      }
      .content th.lexxy-content__table-cell--header {
        background-color: #fafaf9;
        font-weight: 700;
      }
      /* Use a softer divider tone so horizontal rules are present but not visually loud. */
      .content .horizontal-divider {
        margin: 0;
        padding: 16px 0 8px;
      }
      .content .horizontal-divider hr {
        border: 0;
        border-bottom: 1px solid #d6d3d1;
        width: 100%;
        margin: 0;
      }
      .content hr {
        border: 0;
        border-bottom: 1px solid #d6d3d1;
      }
      .btn a {
        background-color: <%= accent_color %>;
        color: #ffffff;
        padding: 8px 16px;
        text-decoration: none;
        border-radius: 8px;
        display: inline-block;
        font-weight: bold;
      }
      .btn-primary a:hover {
        background-color: #27272a;
        border-color: #27272a;
      }
      figure {
        margin: 1em 0;
      }
      img {
        max-width: 600px;
      }
      .footer {
        margin-top: 30px;
        opacity: 0.8;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="content" style="font-family: <%= font_family %>;"><%= @post.content %></div>
      <div class="footer">
        <p><%= sanitize @newsletter.footer_html %></p>
        <p>
          You are receiving this email because you are subscribed to
          <%= @newsletter.title %>
          . Click here to {{unsubscribe_link}}.
        </p>
      </div>
    </div>
  </body>
</html>
