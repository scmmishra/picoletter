<!DOCTYPE html>

<html lang="en">
  <head>
    <% font_family = @newsletter&.font_preference.presence || "sans-serif" %>
    <% primary_color = email_css_color(@newsletter&.primary_color, fallback: "#09090B") %>
    <% button_bg = email_css_color(@newsletter&.theme&.primary, fallback: "#09090B") %>
    <% button_text = email_css_color(@newsletter&.theme&.text_on_primary, fallback: "#FFFFFF") %>
    <% button_hover = email_css_color(@newsletter&.theme&.primary_hover, fallback: "#27272A") %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <%= yield :head %>
    <style>
      body {
        font-family: <%= font_family %>;
        font-size: 14px;
        line-height: 1.4;
        margin: 0;
        -webkit-font-smoothing: antialiased;
        -ms-text-size-adjust: 100%;
        -webkit-text-size-adjust: 100%;
      }
      .container {
        max-width: 600px;
        display: block;
      }
      p {
        font-family: <%= font_family %>;
        font-size: 14px;
        margin-bottom: 14px;
      }
      a {
        color: <%= primary_color %>;
        text-decoration: underline;
      }
      .btn a {
        background-color: <%= button_bg %>;
        color: <%= button_text %>;
        padding: 8px 16px;
        text-decoration: none;
        border-radius: 8px;
        display: inline-block;
        font-weight: bold;
      }
      .btn-primary a:hover {
        background-color: <%= button_hover %>;
        border-color: <%= button_hover %>;
      }
    </style>
  </head>
  <body>
    <table
      role="presentation"
      border="0"
      cellpadding="0"
      cellspacing="0"
      class="body"
    >
      <tr>
        <td class="container"><%= yield %></td>
      </tr>
    </table>
    <div class="footer"><%= yield :footer %></div>
  </body>
</html>
